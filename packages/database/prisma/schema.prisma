generator client {
  provider = "prisma-client-js"
  output   = "../generated/client"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model Serial {
  id          String   @id @default(uuid())
  title       String
  description String
  rating      Float    @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  imageUrl    String   @default("")

  // AniList integration fields
  anilistId          Int? // AniList anime ID
  coverImage         String? // Large cover image URL
  bannerImage        String? // Banner image URL
  titleRomaji        String? // Romaji title from AniList
  titleEnglish       String? // English title from AniList
  titleNative        String? // Native title from AniList
  anilistDescription String? // Rich description from AniList
  lastAnilistSync    DateTime? // When we last synced with AniList

  // ML/AI fields
  embedding Unsupported("vector(512)")? // Aggregated embedding from all episodes (for recommendations)
  tags      String[]                    // Aggregated tags from all episodes

  episodes     Episode[]
  contentItems ContentItem[]
  titleMappings SourceTitleMapping[]
}

model Episode {
  id            String        @id @default(uuid())
  title         String
  url           String        @default("")
  serialId      String
  episodeNumber Int
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  serial        Serial        @relation(fields: [serialId], references: [id])
  contentItems  ContentItem[]
  videoAnalysis VideoAnalysis?

  // ML/AI fields
  embedding Unsupported("vector(512)")? // CLIP embedding for this episode
  tags      String[]                    // Tags for this episode

  @@unique([serialId, episodeNumber])
}

model VideoAnalysis {
  id              String         @id @default(uuid())
  episodeId       String         @unique
  episode         Episode        @relation(fields: [episodeId], references: [id])
  visualEmbedding Float[]
  tags            String[]
  rating          String         @default("safe")
  rawResults      Json
  status          AnalysisStatus @default(PENDING)
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt
}

enum AnalysisStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
}

model Toilet {
  id      String   @id @default(uuid())
  isPaid  Boolean
  price   Int
  address Address?
  reviews Review[]
}

model Address {
  id           String @id @default(uuid())
  streetNumber String
  street       String
  city         String
  country      String
  toiletId     String @unique
  toilet       Toilet @relation(fields: [toiletId], references: [id])
}

model Review {
  id            String   @id @default(uuid())
  cleanliness   Float
  performance   Float
  description   String?  @db.VarChar(1024)
  contributedBy String   @unique
  toiletId      String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  toilet        Toilet   @relation(fields: [toiletId], references: [id])
}

model Account {
  id                String  @id @default(cuid())
  userId            String  @map("user_id")
  type              String
  provider          String
  providerAccountId String  @map("provider_account_id")
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@map("accounts")
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique @map("session_token")
  userId       String   @map("user_id")
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("sessions")
}

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  emailVerified DateTime? @map("email_verified")
  image         String?
  role          UserRole  @default(USER)
  accounts      Account[]
  sessions      Session[]

  @@map("users")
}

model VerificationToken {
  identifier String
  token      String
  expires    DateTime

  @@unique([identifier, token])
  @@map("verification_tokens")
}

enum UserRole {
  USER
  ADMIN
  CONTRIBUTOR
}

model Source {
  id                  String        @id @default(uuid())
  name                String
  type                SourceType
  url                 String
  isActive            Boolean       @default(true)
  autoDownloadEnabled Boolean       @default(false) // Control automatic downloads for new episodes
  lastChecked         DateTime?
  createdAt           DateTime      @default(now())
  updatedAt           DateTime      @updatedAt
  contentItems        ContentItem[]
  titleMappings       SourceTitleMapping[]
}

model SourceTitleMapping {
  id        String   @id @default(uuid())
  sourceId  String?
  rawTitle  String
  serialId  String
  serial    Serial   @relation(fields: [serialId], references: [id])
  source    Source?  @relation(fields: [sourceId], references: [id])
  createdAt DateTime @default(now())

  @@unique([sourceId, rawTitle])
}

model ContentItem {
  id                 String           @id @default(uuid())
  title              String
  description        String?
  url                String
  guid               String           @unique // RSS GUID or unique identifier
  publishedAt        DateTime
  sourceId           String
  serialId           String? // Optional link to serial
  episodeId          String? // Optional link to episode
  infoHash           String? // For torrent content
  downloadedFileName String? // Main video file name after download
  quality            String? // Video quality extracted from title (480p, 720p, 1080p, etc)
  notificationSent   Boolean          @default(false) // Renamed from 'processed' - indicates if notifications were sent
  processingStatus   ProcessingStatus @default(NONE) // New field for video processing workflow
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  source             Source           @relation(fields: [sourceId], references: [id])
  serial             Serial?          @relation(fields: [serialId], references: [id])
  episode            Episode?         @relation(fields: [episodeId], references: [id])
}

model NotificationChannel {
  id        String           @id @default(uuid())
  type      NotificationType
  config    Json // Store channel-specific config (e.g., chat_id for Telegram)
  isActive  Boolean          @default(true)
  createdAt DateTime         @default(now())
  updatedAt DateTime         @updatedAt
}

enum SourceType {
  RSS
  API
  SCRAPER
}

enum NotificationType {
  TELEGRAM
  DISCORD
  EMAIL
}

enum ProcessingStatus {
  NONE // Initial state - item created but not queued
  DOWNLOAD_QUEUED // Download has been queued
  DOWNLOADING // Currently downloading from torrent
  DOWNLOAD_COMPLETED // Download completed, file stored in MinIO
  DOWNLOAD_FAILED // Download failed (can be retried)
  PROCESSING_QUEUED // Queued for video processing (FFmpeg, etc.)
  PROCESSING // Currently being processed (FFmpeg, etc.)
  PROCESSING_COMPLETED // All processing completed successfully
  PROCESSING_FAILED // Processing failed (can be retried)
  PROCESSING_SKIPPED // Item was skipped (e.g., not suitable for processing)
  PENDING_SEEDERS // Download failed due to insufficient seeders, will retry automatically
}
